name: Release

on:
  push:
    tags:
      - '*.*.*-*.*.*'

jobs:
  release:
    uses: FinnSetchell/release-actions/.github/workflows/release.yml@main
    with:
      javaVersion: '17'
    secrets:
      WORKER_API_KEY: ${{ secrets.WORKER_API_KEY }}
